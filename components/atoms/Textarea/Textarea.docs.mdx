---
title: Textarea
description: Multi-line text input with four visual variants, resize control, and error/loading states.
level: atom
---

## Variants

<ComponentPreview code={"import { Textarea } from \"@geeklego/ui\"\n\n<Textarea variant=\"default\" placeholder=\"Default\" rows={3} />\n<Textarea variant=\"filled\" placeholder=\"Filled\" rows={3} />\n<Textarea variant=\"flushed\" placeholder=\"Flushed\" rows={3} />\n<Textarea variant=\"unstyled\" placeholder=\"Unstyled\" rows={3} />"}>
  <div className="flex flex-col gap-4 w-80">
      <div>
        <p className="text-label-sm text-secondary mb-2">Default — outlined border</p>
        <Textarea variant="default" placeholder="Default textarea" rows={3} />
      </div>
      <div>
        <p className="text-label-sm text-secondary mb-2">Filled — muted bg, no border</p>
        <Textarea variant="filled" placeholder="Filled textarea" rows={3} />
      </div>
      <div>
        <p className="text-label-sm text-secondary mb-2">Flushed — border-bottom only</p>
        <Textarea variant="flushed" placeholder="Flushed textarea" rows={3} />
      </div>
      <div>
        <p className="text-label-sm text-secondary mb-2">Unstyled — blank slate</p>
        <Textarea variant="unstyled" placeholder="Unstyled textarea" rows={3} />
      </div>
    </div>
</ComponentPreview>

## Sizes

<ComponentPreview code={"import { Textarea } from \"@geeklego/ui\"\n\n<Textarea size=\"sm\" placeholder=\"Small\" rows={3} />\n<Textarea size=\"md\" placeholder=\"Medium\" rows={3} />\n<Textarea size=\"lg\" placeholder=\"Large\" rows={3} />"}>
  <div className="flex flex-col gap-4 w-80">
      <div>
        <p className="text-label-sm text-secondary mb-2">SM</p>
        <Textarea size="sm" placeholder="Size sm — compact padding" rows={3} />
      </div>
      <div>
        <p className="text-label-sm text-secondary mb-2">MD</p>
        <Textarea size="md" placeholder="Size md — standard padding" rows={3} />
      </div>
      <div>
        <p className="text-label-sm text-secondary mb-2">LG</p>
        <Textarea size="lg" placeholder="Size lg — generous padding" rows={3} />
      </div>
    </div>
</ComponentPreview>

## Disabled

<ComponentPreview code={"import { Textarea } from \"@geeklego/ui\"\n\n<Textarea placeholder=\"Cannot edit\" disabled rows={3} />"}>
  <div className="w-80">
      <Textarea placeholder="Cannot edit this field" disabled rows={3} />
    </div>
</ComponentPreview>

## Error

<ComponentPreview code={"import { Textarea } from \"@geeklego/ui\"\n\n<Textarea\n  id=\"message\"\n  defaultValue=\"x\"\n  error\n  rows={3}\n  aria-describedby=\"message-error\"\n/>\n<p id=\"message-error\" role=\"alert\">Comment must be at least 10 characters.</p>"}>
  <div className="w-80">
      <Textarea
        defaultValue="This field contains an error that must be resolved."
        error
        rows={3}
        aria-describedby="msg-error"
      />
      <p id="msg-error" role="alert" className="text-body-sm mt-1" style={{color: 'var(--color-status-error)'}}>Comment must be at least 10 characters.</p>
    </div>
</ComponentPreview>

## Loading

<ComponentPreview code={"import { Textarea } from \"@geeklego/ui\"\n\n<Textarea placeholder=\"Processing\u2026\" isLoading rows={3} />"}>
  <div className="w-80">
      <Textarea placeholder="Processing…" isLoading rows={3} />
    </div>
</ComponentPreview>

## Resize

<ComponentPreview code={"import { Textarea } from \"@geeklego/ui\"\n\n<Textarea resize=\"none\" placeholder=\"No resize\" rows={3} />\n<Textarea resize=\"vertical\" placeholder=\"Vertical only (default)\" rows={3} />\n<Textarea resize=\"both\" placeholder=\"Any direction\" rows={3} />"}>
  <div className="flex flex-col gap-4 w-80">
      <div>
        <p className="text-label-sm text-secondary mb-2">resize: none</p>
        <Textarea placeholder="Cannot resize" resize="none" rows={3} />
      </div>
      <div>
        <p className="text-label-sm text-secondary mb-2">resize: vertical (default)</p>
        <Textarea placeholder="Resize vertically" resize="vertical" rows={3} />
      </div>
      <div>
        <p className="text-label-sm text-secondary mb-2">resize: both</p>
        <Textarea placeholder="Resize in all directions" resize="both" rows={3} />
      </div>
    </div>
</ComponentPreview>

## Dark Mode

<ComponentPreview code={"<div data-theme=\"dark\" className=\"flex flex-col gap-4 p-8 bg-primary rounded-[var(--radius-component-lg)] max-w-2xl w-96\">\n  <Textarea variant=\"default\" placeholder=\"Default\" rows={3} />\n  <Textarea variant=\"filled\" placeholder=\"Filled\" rows={3} />\n  <Textarea placeholder=\"Error\" error rows={3} />\n  <Textarea placeholder=\"Disabled\" disabled rows={3} />\n</div>"}>
  <div data-theme="dark" className="flex flex-col gap-4 p-8 bg-primary rounded-[var(--radius-component-lg)] max-w-2xl w-96">
      <Textarea variant="default" placeholder="Default" rows={3} />
      <Textarea variant="filled" placeholder="Filled" rows={3} />
      <Textarea variant="flushed" placeholder="Flushed" rows={3} />
      <Textarea variant="default" placeholder="Error state" error rows={3} />
      <Textarea variant="default" placeholder="Loading…" isLoading rows={3} />
      <Textarea variant="default" placeholder="Disabled" disabled rows={3} />
    </div>
</ComponentPreview>

## Props

<PropsTable
  rows={[
    { name: "variant", type: "'default' | 'filled' | 'flushed' | 'unstyled'", defaultValue: "'default'", description: "Visual style variant." },
    { name: "size", type: "'sm' | 'md' | 'lg'", defaultValue: "'md'", description: "Padding and typography scale." },
    { name: "resize", type: "'none' | 'vertical' | 'horizontal' | 'both'", defaultValue: "'vertical'", description: "Resize handle behaviour." },
    { name: "error", type: "boolean", defaultValue: "false", description: "Error state — shows error border and sets aria-invalid." },
    { name: "isLoading", type: "boolean", defaultValue: "false", description: "Loading state — shows spinner and disables interaction." },
    { name: "rows", type: "number", defaultValue: "4", description: "Number of visible text rows." },
    { name: "disabled", type: "boolean", defaultValue: "false", description: "Disables interaction." },
    { name: "placeholder", type: "string", defaultValue: "—", description: "Placeholder text." },
    { name: "wrapperClassName", type: "string", defaultValue: "—", description: "Additional class names for the outer wrapper element." },
    { name: "className", type: "string", defaultValue: "—", description: "Additional class names for the textarea element." },
  ]}
/>

## Accessibility

- Renders a native `<textarea>` — full browser and assistive technology support
- Always associate with a `<label>` via `htmlFor` + `id`, or pass `aria-label` for unlabeled textareas
- `aria-invalid="true"` set when `error` is true — pair with `aria-describedby` pointing to an error message
- `aria-busy="true"` set when `isLoading` is true
- `disabled` sets both the HTML `disabled` attribute and `aria-disabled`

### Keyboard Interaction

| Key | Action |
|-----|--------|
| Tab | Moves focus to the textarea |
| Any key | Types into the field |
